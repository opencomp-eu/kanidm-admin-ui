# Kanidm server URL (must be reachable from this container)
KANIDM_URL=https://kanidm.example.com:8443

# Browser-facing Kanidm URL, used for user-facing links such as password
# resets. Only needed when Kanidm is reached via an internal address.
# KANIDM_PUBLIC_URL=https://kanidm.example.com:8443

# PEM file with a CA certificate to trust in addition to the public roots,
# for Kanidm deployments with a private/self-signed certificate.
# KANIDM_TLS_CA_FILE=/path/to/kanidm-ca.pem

# Service account API token (generate in Kanidm, never expose to browser)
KANIDM_API_TOKEN=your_service_account_api_token_here

# Group whose members may use this admin UI (SPN local part).
# OIDC logins are rejected unless the caller is a member of this group.
KANIDM_ADMIN_GROUP=idm_admins

# External URL where this admin UI is accessible (for OIDC callback)
EXTERNAL_URL=http://localhost:8080

# Server listen address
LISTEN_ADDR=0.0.0.0:8080

# Cookie encryption secret (base64-encoded 32 bytes; generate a random one)
COOKIE_SECRET=changeme_generate_a_random_secret

# --- Optional: OIDC Configuration ---
# If unset, the app runs in dev mode with a simple login bypass.
# For production, configure all three:
# Per-client OIDC issuer (this exact value; the startup check enforces it):
# OIDC_ISSUER_URL=https://kanidm.example.com:8443/oauth2/openid/kanidm_admin_ui
# OIDC_CLIENT_ID=kanidm_admin_ui
# OIDC_CLIENT_SECRET=your_oauth2_client_secret
